analyzers:
    govet:
        extra: true
        exclude: shadow
    errcheck:
    gci:
        Sections: "standard,default,Prefix(github.com/sv-tools/gochecker)"
    gofumpt:
        extra: true
test: true
severity:
    - level: warning
      rules:
          - analyzer: shadow
